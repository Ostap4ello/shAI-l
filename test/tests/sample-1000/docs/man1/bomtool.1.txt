is  a program which generates a textual SPDX 2.0 software bill of
materials (SBOM) for a given set of pkg-config modules.  The out-
put of this tool can then be translated into other  SBOM  formats
as  necessary.  The are as follows: Print the version number, the
Copyright notice, and the license of the program to standard out-
put and exit.  Most other options and all command line  arguments
are ignored.  Print the version number of the program to standard
output  and  exit.  Most other options and all command line argu-
ments are ignored.  If set, print debugging messages  to  stderr.
If  set, ignore rules in modules.  Has the same effect as the op-
tion in A colon-separated list of low-priority directories  where
files  are  looked  up.  The module search path is constructed by
appending this list to which enjoys higher priority.  If  is  not
defined, the default list compiled into the program from the pre-
processor  macro  is  appended instead.  If is defined but empty,
nothing is appended.  Impose a limit on the allowed depth in  the
dependency graph.  A colon-separated list of high-priority direc-
tories  where files are looked up.  Colon-separated list of files
which are loaded before any other pkg-config files.  These  pack-
ages  are  given highest priority over any other files that would
otherwise provide a given package.  Generating an  SBOM  for  the
package named foo:


